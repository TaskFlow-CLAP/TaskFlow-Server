cloud:
  kakao:
    project-id: ${KAKAO_PROJECT_ID}
    region: ${KAKAO_REGION}
    object-storage:
      endpoint: ${KAKAO_OBJECT_STORAGE_ENDPOINT}
      access-key: ${KAKAO_OBJECT_STORAGE_ACCESS_KEY}
      secret-key: ${KAKAO_OBJECT_STORAGE_SECRET_KEY}
      bucket-name: ${KAKAO_OBJECT_STORAGE_BUCKET_NAME}